{% extends 'template.html.twig' %}

{% block title %}Hello BankController!{% endblock %}

{% block main %}



<div class="d-flex flex-wrap">
  {% for account in accounts %}

    <div class="card text-white bg-primary mb-3 mx-4 w-25">
        <div class="card-header bg-dark">{{ account.accountType }}</div>
        <div class="card-body">
        <h4 class="card-title">{{ account.user.lastname }} {{ account.user.firstname }} </h4>
        <p class="card-text mb-3">{{ account.amount }} €</p>
        <div>
            {# vers la page d'un seul account (avec ses operations) #}
            <a  class="btn btn-secondary" href="">Details</a>
            {# vers la page de retrait guichet #}
            <a class="btn btn-success">Retrait</a>
        </div>    
        </div>
    </div>


  {% endfor %}
</div>
  


{% endblock %}
